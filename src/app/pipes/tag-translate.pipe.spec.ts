import { TagTranslatePipe } from './tag-translate.pipe';

describe('TagTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new TagTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
